Instrucciones para subir cambios al git 

cd ruta_donde_queramos_guardar_la_carpeta
git clone https://github.com/aranchaciprian/SmartCityTour_.git
LUEGO MODIFICAMOS LO QUE SEA DE LA CARPETA
cd SmartCityTour_
git checkout -b nombre_rama
git status
git add -A
git status
git commit -m "escribimos_que_hemos_cambiado"
git push origin nombre_rama

###########################################################
INSTRUCCIONES NUEVAS CON RENDER PRIMER ARRANQUE DESDE 0 DESDE ANACONDA PROMPT
# Primero te descargas el repositorio, ósea la carpeta SmartCityTour en algún lugar de tu ordenador
# te copias la ruta donde hayas descargado la carpeta SmartCityTour 
# te vas a anaconda prompt y pones cd ruta que has copiado (como en la primera linea) y la das a enter
# Luego, el resto de líneas incluida flask --app ..., las copias todas y las pegas tal cual en el Anaconda Prompt

cd C:\ruta\SmartCityTour_
conda create --name turismo python=3.10 -y
conda activate turismo
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m flask --app app db upgrade
python scripts\load_tui.py
python scripts\mapa_madrid.py
python -m flask --app app --debug run

#Cuando se termine de ejecutar la ultima linea, te aparece algo como: http://127.0.0.1:5000/ 
#Copias todo esto: http://127.0.0.1:5000/ y lo pegas en el navegador y te sale la plataforma


INSTRUCCIONES VALIDAS ARRENQUE DESDE 0 SI CAMBIO MIGRACIONES
cd C:\ruta\SmartCityTour_
conda create --name turismo python=3.10 -y
conda activate turismo
python -m pip install --upgrade pip
pip install -r requirements.txt
flask --app app db migrate -m "add conversations table"
flask --app app db migrate -m "initial tables"
flask --app app db migrate -m "widen text columns for long urls"
flask --app app db migrate -m "schema update"
flask --app app db upgrade
python scripts\load_tui.py
python scripts\mapa_madrid.py
flask --app app --debug run


ARRANQUE DEL DIA A DIA (sin cambios de modelos/datos/ni python)
cd C:\ruta\SmartCityTour_
conda activate turismo
flask --app app --debug run



###########################################################


GUIA PARA INSTALAR PostgreSQL en WINDOWS POWERSHELL

winget source update
winget search postgresql
winget install -e --id PostgreSQL.PostgreSQL.17
$env:Path += ";C:\Program Files\PostgreSQL\17\bin"
psql --version
psql "DATASET_URL_EXTERNAL se encuentra en el .env"

UNA VEZ INSTALADO, solo abrir WINDOWS POWERSHELL y:
$env:Path += ";C:\Program Files\PostgreSQL\17\bin"
psql --version
psql "DATASET_URL_EXTERNAL se encuentra en el .env"


\conninfo
\dt
\d+ usuarios
\d+ tui
\d+ favoritos
SELECT COUNT(*) FROM usuarios;
SELECT COUNT(*) FROM tui;
SELECT COUNT(*) FROM favoritos;

\copy usuarios   TO 'C:/Users/Arancha/Downloads/usuarios.csv'   WITH (FORMAT csv, HEADER true, ENCODING 'UTF8');
\copy tui        TO 'C:/Users/Arancha/Downloads/tui.csv'        WITH (FORMAT csv, HEADER true, ENCODING 'UTF8');
\copy favoritos  TO 'C:/Users/Arancha/Downloads/favoritos.csv'  WITH (FORMAT csv, HEADER true, ENCODING 'UTF8');

###########################################################

